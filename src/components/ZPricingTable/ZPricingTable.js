import React, { PropTypes } from 'react';
import withStyles from 'isomorphic-style-loader/lib/withStyles';
import s from './ZPricingTable.css';

class ZPricingTable extends React.Component {
    constructor(props) {
        super();
    };

    render() {
        return (
            <div>

            </div>
        );
    };
};

export default withStyles(s)(ZPricingTable);
